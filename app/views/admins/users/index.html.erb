<h2>ユーザ一覧</h2>
  <%= form_with url: admins_search_user_path, method: :get, local: true do |f| %>
    <%= f.text_field :word, placeholder: '名前かメールアドレス' %>
    <%= f.submit "検索" %>
  <% end %>
<%= render 'index', users: @users %>