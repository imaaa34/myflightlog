<% if log.favorited_by?(current_user) %>
  <%= link_to '★', log_favorites_path(log), method: :delete, remote: true, class:'favorite-btn' %>
<% else %>
  <%= link_to '☆', log_favorites_path(log), method: :post, remote: true, class:'favorite-btn' %>
<% end %>