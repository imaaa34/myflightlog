<div class="container my-5">
  <div class="login-background col-lg-7 text-center mx-auto">
    <h2>新規登録 <i class="fas fa-plane fa-xs"></i></h2>

    <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
      <%= render "public/shared/error_messages", resource: resource %>

      <div class="field mt-4">
        <%= f.text_field :name, placeholder: '名前', autofocus: true %>
      </div>

      <div class="field mt-4">
        <%= f.email_field :email, placeholder: 'メールアドレス', autofocus: true, autocomplete: "email" %>
      </div>

      <div class="field mt-4">
        <%= f.password_field :password, placeholder: 'パスワード(6文字以上)', autocomplete: "new-password" %>
      </div>

      <div class="field mt-4">
        <%= f.password_field :password_confirmation, placeholder: '確認用パスワード', autocomplete: "new-password" %>
      </div>

      <div class="actions mt-3">
        <%= f.submit "新規登録", class: 'btn btn-outline-secondary' %>
      </div>
    <% end %>
    <p class="mt-3">
      アカウントをお持ちの方は<%= link_to "こちら", new_user_session_path %>からログインしてください。<br>
      <%= link_to 'googleアカウント', user_google_oauth2_omniauth_authorize_path, method: :post %>で新規登録することもできます。
    </p>
  </div>
</div>
