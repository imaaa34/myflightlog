<div class="container my-5">
  <div class="login-background col-lg-7 text-center mx-auto">
    <h2>ログイン <i class="fas fa-plane fa-xs"></i></h2>

    <%= form_with model: @user, url: user_session_path, local: true do |f| %>
      <div class="field mt-4">
        <%= f.email_field :email, placeholder: 'メールアドレス', autofocus: true, autocomplete: "email" %>
      </div>

      <div class="field mt-4">
        <%= f.password_field :password, placeholder: 'パスワード', autocomplete: "current-password" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :パスワードを記憶する %>
        </div>
      <% end %>

      <div class="actions mt-3">
        <%= f.submit "ログイン", class: 'btn btn-outline-secondary' %>
      </div>
    <% end %>
    <p class="mt-3">
    アカウントをお持ちでない方は<%= link_to 'こちら', new_user_registration_path %>から新規登録をお願いします。<br>
    <%= link_to 'googleアカウント', user_google_oauth2_omniauth_authorize_path, method: :post %>でログインすることもできます。
    </p>
  </div>
</div>